# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you: 
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Kernyan's blog
description: >- # this means to ignore newlines until "baseurl:"
  Notes on OS development, reverse-engineering, and whatever cool stuff I find
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://www.kernyan.com" # the base hostname & protocol for your site
github_username:  kernyan

# Build settings
theme: minima
plugins:
  - jekyll-feed
  - jekyll-sitemap

# Google Analytics
google_analytics: G-T4JBRGCHJZ

# MathJax Configuration
mathjax: true

# Giscus Comments Configuration
giscus:
  repo: "kernyan/kernyan.github.io"
  repo_id: "R_kgDOGn16PA"
  category: "Announcements"
  category_id: "DIC_kwDOGn16PM4CsYBP"
  mapping: "pathname"
  reactions_enabled: 1
  emit_metadata: 0
  input_position: "bottom"
  theme: "preferred_color_scheme"
  lang: "en"

# Custom navigation categories
sidebar_categories:
  - name: "C/C++"
    slug: "c-c"
  - name: "CPU" 
    slug: "cpu"
  - name: "CUDA"
    slug: "cuda"
  - name: "Hardware"
    slug: "hardware"
  - name: "Others"
    slug: "others"
  - name: "OS"
    slug: "os"
  - name: "Robotics"
    slug: "robotics"
  - name: "Windows"
    slug: "windows"

# Show excerpts on home page
show_excerpts: true

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
exclude:
  - .sass-cache/
  - .jekyll-cache/
  - gemfiles/
  - Gemfile
  - Gemfile.lock
  - node_modules/
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
  - ruby/3.0.0/gems/
  - ruby/
